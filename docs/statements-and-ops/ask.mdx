---
title: "Ask"
---

`ASK(X,Y)`: This is a table-valued function. Ask question X over corpus Y (i.e.
a column of varchar or URI). Returns `LIMIT #` of `VARCHAR` answers from the
corpus, in the form of `(answer, row_id)` pairs.

Usable in: `FROM`
