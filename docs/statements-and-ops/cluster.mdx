---
title: "Cluster"
---

`CLUSTER(X)`: This is an element-wise column operation. This operation clusters
the content in the column by semantic similarity. Returns an INT column of
cluster ids.

### Example

```sql
SELECT CLUSTER(review_text) FROM product_reviews_table;
```
